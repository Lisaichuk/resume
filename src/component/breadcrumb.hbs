<div class="row col-12 g-2">

    <div class="col-6 ms-3">

        <div class="row">
            <div class="col-12">
                <ol class="breadcrumb">
                    {{#each breadcrumb}}
                    <li class="breadcrumb-item">
                        <a href="{{url}}">{{name}}</a>
                    </li>
                    {{/each}}
                </ol>
            </div>
        </div>
    </div>
</div>